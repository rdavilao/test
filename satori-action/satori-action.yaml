name: 'Chatbot quality assurance'
description: 'Apply several techniques to check the quality of your chatbot'
inputs:
  owner:
    description: 'Username of the repository owner's'
    default: ${{ github.repository_owner }}
  files: 
    description: 'Files to be checked'
    required: true
    default: 'A test'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - xxyy
    - ${{ inputs.files }}
    - ${{ inputs.owner }}
