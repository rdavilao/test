name: test-satori-action
run-name: ${{ github.actor }} is checking chatbots with SATORI
on: [push]
jobs:  
  check-chatbot:
    runs-on: ubuntu-latest
    steps:
      - uses: ./satori-action/.
        with:
          format: "Dialogflow"
